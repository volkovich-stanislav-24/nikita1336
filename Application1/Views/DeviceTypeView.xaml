<Grid x:Class="Application1.Views.DeviceTypeView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    mc:Ignorable="d"
    xmlns:vs="clr-namespace:Application1.Views"
    x:Name="root"
    d:DesignHeight="128" d:DesignWidth="128"
    DataContext="{Binding ViewModel, RelativeSource={RelativeSource Self}}"
>
    <Grid.RowDefinitions>
        <RowDefinition/>
        <RowDefinition Height="auto"/>
    </Grid.RowDefinitions>
    <Image Source="{Binding Image, Mode=OneTime}" Grid.Row="0"/>
    <TextBlock Text="{Binding Name, Mode=OneTime}" HorizontalAlignment="Center" Grid.Row="1"/>
</Grid>
